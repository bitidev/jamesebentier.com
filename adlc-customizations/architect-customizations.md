# Architect Customizations

Project-specific overrides to the architect's default behavior. **Empty by default** — the architect uses its role definition as-is; this file adds nothing until you add rules below. To author customizations, see the ADLC framework README, "Customizing Agents".
