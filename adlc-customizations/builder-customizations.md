# Builder Customizations

Project-specific overrides to the builder's default behavior. **Empty by default** — the builder uses its agent definition as-is; this file adds nothing until you add rules below. To author customizations, see the ADLC framework README, "Customizing Agents".
