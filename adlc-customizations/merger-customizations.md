# Merger Customizations

Project-specific overrides to the merger's default behavior. **Empty by default** — the merger uses its role definition as-is; this file adds nothing until you add rules below. To author customizations, see the ADLC framework README, "Customizing Agents".
