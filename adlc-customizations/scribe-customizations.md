# Scribe Customizations

## Upstream / jb-brown quarantine (HARD)

- Specs and docs for this project live under **this** repo’s `docs/` — never under the `adlc/` symlink, and never as contributions intended for jb-brown / Invoca-ADLC.
- Do not create ADLC framework changelog/spec entries aimed at upstream. Local process notes go in this consumer’s `docs/` or `adlc-customizations/`.
- Ignore framework dogfood guidance that says “edit in `adlc/`” when working inside this consumer repo; that guidance applies only inside a personally owned framework checkout used as the product under development.
