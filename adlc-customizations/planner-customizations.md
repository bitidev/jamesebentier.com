# Planner Customizations

Project-specific overrides to the planner's default behavior. **Empty by default** — the planner uses its agent definition as-is; this file adds nothing until you add rules below. To author customizations, see the ADLC framework README, "Customizing Agents".
